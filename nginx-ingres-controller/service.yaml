apiVersion: v1
kind: Service
metadata:
  name: go-api-service
spec:
  selector:
    app: go-api # This must match the 'labels' in your Deployment
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080 # The port your Go app listens on
